<?xml version="1.0" encoding="utf-8"?>
<project>
  <!-- These values are added to the project XML when you include this library. -->
  <haxelib name="jsonpath" />
  <haxelib name="jsonpatch" />
  <haxelib name="thx.core" />
  <haxelib name="thx.semver" />

  <!--
    Macro flags should go into the "extraParams.hxml" file instead of this one.
    This is because 'include.xml' is exclusive to the OpenFL enviroment.
  -->
</project>
